# Optional runtime secret; never put credentials into a Docker build argument.
HF_TOKEN=
HF_HOME=/app/cache/huggingface
# Required only for online W&B logging. Never commit a populated .env file.
WANDB_API_KEY=
# Optional reasoning judge (Grok 4.6, low reasoning).
GROK_API_KEY=
